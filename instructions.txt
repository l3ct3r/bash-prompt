Perform the followling instructions: 

$ sudo apt install neofetch
$ nano .bashrc 
(Paste the script below # alias mv = 'mv -i')
(Place neofetch on top of .bashrc)

$ sudo su 
# sudo apt install fonts-powerline 
# nano /root/.bashrc
(Paste the script below # alias mv = 'mv -i')
(Place neofetch on top of .bashrc)
# exit 
( Clear and reload the Terminal) 
